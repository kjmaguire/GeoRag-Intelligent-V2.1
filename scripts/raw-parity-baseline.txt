# Objects that database/raw/ creates and no migration does.
#
# CD deploys by running `php artisan migrate` and nothing else, so every line
# here is an object that has never existed on Azure — while live code queries
# several of them (routers/interpretation.py reads all four interpretation.*
# tables; the phase0 ops agents read silver.corpus_health_findings and
# silver.store_reconciliation_findings; app/agent/egress_gate.py reads
# silver.workspace_settings).
#
# 2026-08-28: 22 entries closed by 2026_08_28_100000..100500 —
#   100000..100300  the operational core: outbox.*, the six Layer-E workspace
#                   tables, the four tool-gateway tables, feature_flags + its
#                   history table and audit trigger. The `outbox` SCHEMA went
#                   with them: it was created only by
#                   docker/postgresql/init/10-phase0-extensions-and-schemas.sql,
#                   a Docker image init script Azure's managed Postgres never runs.
#   100400          the four §19.3 interpretation.* tables (and that schema).
#   100500          the three Layer-G silver findings tables.
#
# None of these ports is a transcription. Each batch turned up at least one
# place where copying the raw DDL verbatim would have shipped something wrong,
# because later migrations had already reconciled the table and are guarded on
# it existing — so they ran, skipped, and will never fire again:
#   - the four tables in the 2026_08_14_030000 fail-closed subset had to be
#     created CLOSED, not in the raw file's fail-open shape;
#   - every table created after the one-shot 2026_08_24_010000 sweep needs an
#     explicit FORCE ROW LEVEL SECURITY, which the raw files mostly omit;
#   - interpretation_comments needed a workspace_id index its siblings have;
#   - corpus_health_findings.workspace_id had to be created NULLABLE
#     (2026_08_19_070000);
#   - storage_tier_policy needed the canonical single policy and the
#     NULLS NOT DISTINCT constraint (2026_08_25_200000);
#   - store_reconciliation_findings.drift_type had to admit 'cross_store_drift',
#     which its writer emits and the raw CHECK rejects.
# Read the reconciliations before porting the remaining 11.
#
# ONE MORE TRAP, for whoever ports the rest. The `Migrations under production
# privileges` CI job applies database/raw/phase0/*.sql as `bootstrap`, then
# migrates as `georag_ci` — deliberately not a member of bootstrap, never a
# superuser. Only three phase0 files CREATE SCHEMA, and measured against that
# job, two of them land: bootstrap ends up owning schema `interpretation` (4
# tables) and schema `backups` (1 table). A migration that CREATEs a table in
# a foreign-owned schema does NOT quietly skip on IF NOT EXISTS — Postgres
# resolves the creation namespace first and raises `permission denied for
# schema`. 2026_08_28_100400 hit exactly this and now stands down with a
# RAISE WARNING instead (note its ownership probes go through pg_class by
# NAME, since to_regclass() itself needs USAGE on the schema).
# backups.snapshot_runs is the remaining entry that will hit the same wall.
#
# 2026-08-28 (batch 4): 3 more closed, 11 -> 8.
#   2026_08_28_100600  silver.claim_ledger + silver.workspace_settings.
#   silver.structure_measurements closed by ARCHIVING, not porting. Its raw
#   file had carried an "ARCHIVED 2026-07-03 - never applied to any cluster;
#   do NOT run" header for two months, stating that archiving it "removes it
#   from the rollup glob" - but the file was still sitting in
#   database/raw/phase5/, which scripts/phase4_step7_build_rollup.sh matches
#   with database/raw/phase[0-9]*. The move was never performed. Its sibling
#   phase5/10-drillhole-intervals-visual.sql carried the identical header and
#   the identical omission; the third of the trio HAD been moved, so this was
#   a cleanup that stopped one file in. Both are now in database/raw/_archive/
#   (which this gate excludes via EXCLUDED_DIRS), matching the convention
#   phase5-20-cross-section-panels.sql already set. Neither was in
#   manifest.json, so db:apply-raw is unaffected.
#
# 2026-08-28 (batch 5): 2 more closed, 8 -> 6, by 2026_08_28_100700.
#   audit.integration_credentials_audit was NOT dormant work. It is the one
#   relation raw/phase0/98-rls-tenant-isolation-block3.sql operates on that no
#   other path creates, and 98 IS manifest entry 4 - so db:apply-raw runs it.
#   98 is wrapped BEGIN;(36) ... COMMIT;(419) and ApplyRawSql runs it whole,
#   so the missing table made the WHOLE FILE a no-op, not just the statements
#   after it: reproduced on PG16, the 42P01 aborts the transaction and the
#   closing COMMIT executes as a ROLLBACK, taking back the statements that had
#   already succeeded. All 65 of block 3's ALTER/CREATE POLICY/CREATE INDEX/
#   UPDATE statements. The manifest's `requires` guard exists to prevent
#   exactly this and simply had not been told about the table; it is now
#   listed there too.
#   backups.snapshot_runs needed the same foreign-ownership stand-down as
#   interpretation (predicted above, and confirmed: `permission denied for
#   schema backups` for a naive CREATE TABLE as georag_ci).
#
# THE REMAINING 6 ARE NOT A TO-DO LIST. None should be closed by porting:
#
#   gold.h3_density_mineral + function silver.density_choropleth_h3
#     NOT PORTABLE, and porting them would break `php artisan migrate` on
#     Azure. The table's h3_index column has type `h3index`, supplied by the
#     `h3` extension, and routers/visualizations.py::_h3_capability_missing
#     already documents why that is permanent: h3 is absent from Azure
#     Flexible Server's azure.extensions allowedValues, so CREATE EXTENSION h3
#     can never succeed there regardless of privileges. A migration creating
#     either object would fail with `type "h3index" does not exist` and stop
#     the chain. The heatmap is capability-gated in the router precisely
#     because of this, and test_target_heatmap_capability_gate.py pins it.
#     SEPARATE DEFECT, unrelated to deployment: density_choropleth_h3 calls
#     silver.h3_cell_to_boundary_geometry(d.h3_index) twice, and that function
#     is not defined anywhere in this repository. It is LANGUAGE plpgsql, so
#     CREATE succeeds and only invocation fails - which is why nobody has hit
#     it. The function is incomplete even on a cluster that HAS h3.
#
#   [CLOSED 2026-08-28, batch 6] silver.assays + silver.assay_samples were
#     archived, not ported, and their five consumers repointed at
#     silver.assays_v2. The deciding evidence was georag-architecture.html,
#     the stated source of truth: it names silver.assays_v2 and silver.samples
#     and never mentions silver.assays or silver.assay_samples at all.
#     Consumers fixed: CsvAssaysExporter.php (was also joining silver.samples,
#     which assays_v2 makes unnecessary - it carries collar_id/from_depth/
#     to_depth itself) and four charts in routers/visualizations.py (harker,
#     spider, ree_pattern, grade_tonnage). All four were serving FABRICATED
#     demo data on Azure, because the 42P01 was swallowed by the blanket
#     `except Exception` that falls back to placeholders - the exact thing the
#     target_heatmap gate refuses to do, but for a permanent cause.
#     Two things a mechanical remap would have got wrong, both measured:
#       - assays_v2 has no rock_type, so Harker takes it from the
#         silver.lithology interval containing the sample's from_depth
#         (LEFT JOIN, so unmatched samples still fall through to "unknown").
#       - the legacy qc_flag/below_detection columns were NOT NULL, so
#         `!= 'rejected'` and `= false` were safe. Their assays_v2 equivalents
#         are nullable, where those predicates are NULL and drop the row.
#         Measured on a fixture: 3 rows vs 5, and 4 vs 6. Both filters now use
#         IS DISTINCT FROM.
#     STILL STALE, deliberately not touched: eval_seeders/numeric_grounding.py
#     and schema_mapping.py carry "silver.assays" as declarative source_table /
#     canonical_table metadata in golden-query expectations. It is inert (never
#     executed as SQL), and golden query sets are a milestone gate owned by
#     test-engineer, so retargeting them is an SME call, not a rename.
#
#   [CLOSED 2026-08-28, batch 6] workflow.activepieces_channels and
#     silver.shadow_runs were both archived rather than ported. Their whole
#     declaring files turned out to be fully superseded, so each moved to
#     database/raw/_archive/ intact:
#       phase0/101 declared qp_credentials (migration 2026_05_14_140200),
#         workspace_settings (2026_08_28_100600) and activepieces_channels
#         (DROPped by 2026_05_17_120000) -- all three covered.
#       phase1/20 declared shadow_runs (DROPped by raw/phase4/90, no live
#         reader or writer) and workspace.feature_flags (2026_08_28_100300).
#     docs/phase_h4_deploy_checklist.md told an operator to
#     `psql < database/raw/phase0/101-...sql`; that step is now marked obsolete
#     rather than left pointing at a moved file.
#
# This file is a to-do list, not a permission slip. The gate
# (scripts/check-raw-migration-parity.php) fails on anything NEW, and also
# fails when a line here is closed but left behind — so the list can only
# shrink. Close one by porting the DDL into database/migrations/, or by
# adding its file to database/raw/manifest.json if it is genuinely
# deploy-time raw SQL that `php artisan db:apply-raw` should carry.
#
# Regenerate with: php scripts/check-raw-migration-parity.php --update-baseline
#
# 2026-08-25: detection sharpened from "a migration mentions the name" to "a
# migration CREATEs the object" (see the gate's docblock). That restored 12
# entries the substring match had silently closed or never tracked — including
# silver.storage_tier_policy, whose entry PR #188 was forced to remove when an
# ALTER-only migration landed. Two of the restored entries are special:
#   - silver.assays: was masked because "silver.assays" is a substring of
#     "silver.assays_v2" (which migrations do create). assays_v2 is the
#     canonical table — closing this entry likely means archiving the legacy
#     raw DDL, not porting it.
#   - workflow.activepieces_channels: deliberately DROPPED in production by
#     2026_05_17_120000, but raw/phase0/101 still creates it locally. Close
#     by removing/archiving the raw DDL, not by porting it.

function silver.density_choropleth_h3  # raw/phase0/105-section6-density-mvt-function.sql
table gold.h3_density_mineral  # raw/phase0/104-section6-h3-density-table.sql
