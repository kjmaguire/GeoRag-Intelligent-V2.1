# §04f-pg Addendum — MERGED into the architecture doc on 2026-05-07

This addendum has been merged into the canonical architecture reference.

**Canonical location:** open `georag-architecture.html` and search for the heading
**"04f-pg — Knowledge Graph: Public Geoscience Extension (Neo4j)"**
(immediately follows §04f, immediately precedes §04g).

This stub remains only as a backlink target for any external reference that
points at this filename. **Do not edit this file** — edit the HTML doc instead.
The single source of truth is the HTML; keeping a parallel markdown copy was
the source of the §04f drift class this very addendum was written to close.

## Audit trail of what was merged (2026-05-07 session)

- 9 additional Public Geoscience node types documented (`:Jurisdiction`,
  `:PublicGeoSource`, `:Commodity`, `:Mine`, `:MineralOccurrence` PG variant,
  `:DrillHole` PG variant, `:ResourcePotentialZone`, `:RockSample`,
  `:AssessmentSurvey`).
- 9 additional relationships documented (`PUBLISHED_BY`, `SOURCED_FROM`,
  `HAS_PRIMARY_COMMODITY`, `HAS_ASSOCIATED_COMMODITY`, `HAS_COMMODITY`,
  `COVERS_AREA_FOR`, `LOCATED_IN`, `REFERENCES`, `PAYLOAD_TEXT_SNIPPET`).
- Secondary-label pattern (`:PublicGeo` umbrella) documented as a key-note.
- D2-followup regression bug documented (8 lines fixed across 2 files).
- Regression CI guard documented:
  `src/fastapi/tests/test_no_legacy_drillhole_label.py`.
- 4 open questions for Kyle preserved in the HTML as "Pending SME review".

## Outstanding action items (not closed by the merge)

1. **Maintenance-window task:** re-run
   `ops/migrations/neo4j/2026-04-27-drillhole-rename.cypher` to canonicalise
   any `:Drillhole` nodes the regression created since 2026-04-27.
2. **SME review:** Kyle to answer the 4 open questions in §04f-pg.
3. After (2): edit §04f-pg to remove the "Pending SME review" paragraph and
   replace with the resolved decisions.
