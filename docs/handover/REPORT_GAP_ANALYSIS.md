# Report Gap Analysis
_Generated by Claude Code gap sweep — 2026-05-29_

## Summary

Total gaps found: **19** (Critical: 3, Moderate: 9, Minor: 7, Confirmed accurate: 20 areas).

**Sweep methodology:** read all 5 handover docs (HANDOVER_INDEX.md, SAD.md, DFS.md, API_DOCUMENTATION.md, CICD_PIPELINE.md) in full, then ran 12 categorical verification sweeps against the live codebase under `C:\Users\GeoRAG\Herd\georag\`. Every "missing" claim was cross-checked by grep against the doc text before flagging. Counts verified with `wc -l` / `grep -c`.

---

## Critical Gaps (missing entirely — would materially affect a reader's understanding)

### C1 — Reranker model identity mismatch
- **Category:** 10. LLM / ML pipeline
- **Location:** `src/fastapi/app/services/reranker.py`
- **What it is:** Live code pins `RERANKER_MODEL_NAME = "BAAI/bge-reranker-base"` (revision `2cfc18c9415c…`, score range [-1, +1] logit). SAD §2.1 + §3.3 + §3.3.5 + DFS all say reranker = `cross-encoder/ms-marco-MiniLM-L-6-v2`.
- **Why missed:** Carried forward from an earlier model decision; never propagated when reranker was switched to bge-reranker-base. Memory note "Reranker overnight 2026-05-29" + ADR-0011 both reference bge-reranker-base; handover never re-synced.
- **Why it matters:** Two different models with different score-normalisation regimes. Anyone setting up the reranker tier from the handover will pull the wrong weights.

### C2 — `georag_app` and `martin_ro` PG roles never created in init scripts
- **Category:** 9. Security
- **Location:** `docker/postgresql/init/init-roles.sql` + entire `docker/postgresql/init/` tree
- **What it is:** `init-roles.sql` only creates `georag_read`, `georag_write`, `georag_audit` (all NOLOGIN). 30+ migrations call `GRANT ... TO georag_app;` and Martin's `martin_ro` role is named in SAD §4.2 + API §7 + DFS §4.1. Neither role is provisioned by any file under `docker/postgresql/` or `database/raw/phase0/`.
- **Why missed:** Existing `project_init_roles_gap` memory note flagged placement; the deeper gap is **non-existence**. CICD §6.2 init sequence doesn't show where they come from.
- **Why it matters:** Fresh cluster's `artisan migrate --database=pgsql_migrations` will fail on first GRANT statement.

### C3 — Fusion algorithm (RRF) not documented anywhere
- **Category:** 7. Retrieval pipeline
- **Location:** `src/fastapi/app/services/fusion.py`
- **What it is:** `RRF_K = 60`, formula `score(d) = Σ 1/(k + rank(d))`, with per-list weighting via `RetrievalProfile.bm25_weight` (0.0 = dense-only … 1.0 = sparse-only).
- **Why missed:** SAD §2.1 names dense/sparse/graph technologies and DFS §2.3 says "hybrid search (dense + sparse)" but the actual cross-store score-combination math appears in **none** of the 5 handover files.
- **Why it matters:** This is the core of how RAG ranking works — without it nobody can reason about ranking changes, tune the bm25_weight knob per intent, or debug "why did this chunk rank here".

---

## Moderate Gaps (covered superficially — named but not explained)

### M1 — Layer 6 constraint numeric limits not documented
- **Category:** 10. LLM / ML pipeline
- **Location:** `src/fastapi/app/agent/hallucination/layer6_constraints.json`
- **What it is:** Actual SME-editable thresholds: `depth_max_m=5000.0`, `grade_gold_max_ppm=1000.0`, `grade_uranium_max_pct`, `recovery_max_pct`, `azimuth_range`, `dip_range`, `rqd_range`.
- **What's missing:** SAD §4.1 lists the 7 rule names but no numeric values. Reader cannot tell what triggers `constraint_violation`.

### M2 — Model-routing tier system undocumented
- **Category:** 10. LLM / ML pipeline
- **Location:** `src/fastapi/app/config.py` + `src/fastapi/app/agent/model_routing.py`
- **What it is:** `MODEL_TIER_FAST="claude-haiku-4-5"`, `MODEL_TIER_STANDARD="claude-sonnet-4-6"`, `MODEL_TIER_DEEP="claude-opus-4-8"`. Functions `select_tier()`, `downshift()`, `is_retriable_via_failover()`, classifier-fallback → DEEP rule. (STANDARD bumped 4-5→4-6 and DEEP bumped 4-7→4-8 in the 2026-06 audit sweep.)
- **What's missing:** SAD §4.4 + Index §5.7 only name `MODEL_ROUTING_ENABLED`; the actual 3-tier logic + tier→model mapping + downshift rules are invisible.

### M3 — Per-intent retrieval profile shape not documented
- **Category:** 7. Retrieval pipeline
- **Location:** `src/fastapi/app/agent/agentic_retrieval/retrieval_profile.py`
- **What it is:** 8 fields per intent: `primary_tools`, `secondary_tools`, `bm25_weight`, `conflict_detection_enabled`, `adversarial_pass_enabled`, `surface_qa_qc_fields`, `require_regulatory_constraints`, `answer_emphasis` (6-value Literal), `max_chunks`.
- **What's missing:** SAD §3.3.2 only says "per-intent retrieval profile (k, fusion weights, rerank toggle, parent expansion, MMR)". The actual 8 intents × 8 fields table is the canonical retrieval contract.

### M4 — Reranker per-class top-K + batch policy undocumented
- **Category:** 7. Retrieval pipeline
- **Location:** `src/fastapi/app/services/reranker.py`
- **What it is:** `RERANKER_TOP_K_BY_CLASS` table — factual:20, spatial:30, document:15, computation:10, viz:30, unknown:20. `RERANKER_TIMEOUT_S = 2.0` for 50-candidate batches.
- **What's missing:** SAD §3.3.5 covers history (HOLD verdict, MLM-adapted checkpoint) but not the canonical per-class table that drives current production retrieval.

### M5 — `silver_to_gold/` Dagster subdir not catalogued
- **Category:** 6. Dagster assets
- **Location:** `src/dagster/georag_dagster/assets/silver_to_gold/`
- **What it is:** 7 modules: `assay_composites`, `campaign_summaries`, `drill_summaries`, `element_correlations`, `qaqc_statistics`, `significant_intersections`, `zone_statistics`.
- **What's missing:** DFS §2.1 + Index §4 say "53 + 4 (bronze_to_silver)" — the `silver_to_gold/` subdir is real and orphan in the handover.

### M6 — Dagster asset check count drift
- **Category:** 6. Dagster assets
- **Location:** `src/dagster/georag_dagster/checks/`
- **What it is:** Live `grep -c @asset_check` = **29**; handover says **27** (CICD §6.8, DFS §2.1).
- **What's missing:** Likely from `interval_overlap.py` + `interval_overlap_checks.py` pair. Both files + their assertions need to be acknowledged.

### M7 — Hatchet workflow module count drift
- **Category:** 5. Hatchet workflows
- **Location:** `src/fastapi/app/hatchet_workflows/`
- **What it is:** Live = **47** `.py`; handover says **45** (Index §4, SAD §3.4).
- **What's missing:** Two extra workflow modules need identifying + categorising into the 30 cron schedules + on-demand inventory.

### M8 — Route count drift
- **Category:** 1. API surface
- **Location:** `routes/api.php` + `routes/web.php`
- **What it is:** Live = **74 api + 161 web**; handover says **67 + 157** (Index §4, API §3, SAD §3.2).
- **What's missing:** API §3 inventory tables likely missing 7 + 4 recent routes.

### M9 — Event payload field shapes not documented
- **Category:** 1. API surface
- **Location:** `app/Events/` (11 ShouldBroadcast classes)
- **What it is:** All 11 events declare `broadcastWith(): array`. API §6.2 lists channels + `broadcastAs()` strings but never enumerates the payload field shape.
- **What's missing:** Frontend hooks like `useWorkspaceDataUpdated` key off specific fields (`data_version`, `project_id`) — contract not documented.

---

## Minor Gaps (complete enough for most readers, but could be expanded)

### m1 — Env keys read by `config/*` but not in `.env.example`
- **Location:** `config/*.php` vs `.env.example`
- **What it is:** 89 keys (300 distinct config reads vs 211 in .env.example). Many are framework defaults (SESSION_*, AUTH_GUARD) but GeoRAG-relevant ones present: `SANCTUM_TOKEN_PREFIX`, `AUTHZ_AUDIT_RETENTION_DAYS`, `REVERB_SCALING_ENABLED`/`_CHANNEL`, `SLACK_BOT_USER_DEFAULT_CHANNEL`.

### m2 — `geological_query_expansion.expand_query()` role not pinned
- **Location:** `src/fastapi/app/services/geological_query_expansion.py`
- **What it is:** `expand_query(query, *, max_expansions=6) -> str` — named but role in pipeline (pre-retrieval rewrite vs post-retrieval reformulation) not specified.

### m3 — Frontend components named only, not described
- **Location:** `resources/js/Components/{Foundry, GuardError, HoleAnalysis, Analytics, PublicGeoscience, ui, chat}/` (~50 .tsx files)
- **What it is:** `GuardError/{AmbiguityPicker, DepthPickerCard, UnitPickerCard, ConflictSideBySide, …}` map 1:1 to refusal codes in API §8.1 — mapping not documented.

### m4 — Hook behaviour listed by name only
- **Location:** `resources/js/Hooks/`
- **What it is:** 8 top-level + 4 sub-dir hooks. `Dashboard/{useDashboardFetch, useFreshnessWarning}` and `PublicGeoscience/{useFeatureDetail, useJurisdictions}` not surfaced at all.

### m5 — FormRequest field-level rules only documented for 3 of 13
- **Location:** `app/Http/Requests/`
- **What it is:** Missing rule coverage for: `StoreCollar`, `StoreProject`, `UpdateProject`, `StoreVendorProfile`, `UpdateVendorProfile`, `StoreColumnMapping`, `UpdateColumnMapping`, plus 4 under `Admin/AgentConfig/`.

### m6 — `database/raw/{_adhoc, phase0..phase30}` SQL files (61 files) under-referenced
- **Location:** `database/raw/`
- **What it is:** Carry RLS / function / trigger DDL parallel to Laravel migrations; named once in handover. Should be surfaced alongside migrations as source-of-truth.

### m7 — Index strategy undocumented
- **Location:** `database/migrations/` + `database/raw/`
- **What it is:** 42 `->index()`/`->unique()` in Laravel migrations + ~539 `CREATE INDEX` in raw SQL.
- **What's missing:** DFS §4 covers triggers/functions/extensions but no index summary or strategy section.

---

## Confirmed Accurate (spot-checked and matches handover)

- 42 `@georag_agent` agents (count + per-phase breakdown)
- 64 alerts across 13 rule files
- 14 + 3 product Grafana dashboards
- 11 Event classes
- CSP allowlist directives
- 7 Layer 6 constraints (names — not values, see M1)
- 25 refusal codes
- 4 authz gates
- Encrypted PII on `QueryAuditLog`
- 10 export formats
- HMAC canonical-JSON serialisation (`json.dumps(..., sort_keys=True)` confirmed)
- 3 Kestra flows
- 5 Dagster resources (`PostgresResource`, `S3Resource`, `QdrantResource`, `Neo4jResource`, `VllmResource`)
- 5 Dockerfiles
- 8 + 8 MVT functions (silver + public_geo)
- bge-small + SPLADE++ model names
- `init-roles.sql` IS in init dir (the earlier `project_init_roles_gap` memory note is now stale on placement — but see C2 for the deeper gap on which roles exist)
- `authz_audit` single dedicated Log channel
- 12-field ContextEnvelope validation rules
- 6-layer hallucination file inventory

---

## Unverified

- Live PG `information_schema` counts (handover itself flags 174 vs 248 drift in §5.1)
- Per-line Hatchet cron expression accuracy against the live `definitions.py` declarations
- OpenAPI snapshot 10-of-109 endpoint coverage drift since last snapshot
- Caddy → Kestra round-trip on `/admin/integrations/kestra/{path?}`
- Complete enumeration of workspace-flag check sites across `app/` + `src/fastapi/`

---

*Generated by Claude Code gap sweep — 2026-05-29. Source documents at `docs/handover/`. Live codebase: `C:\Users\GeoRAG\Herd\georag\`.*
