# System prompt token budget audit — TEMPLATE

**Status:** Template only — `scripts/measure_system_prompt_tokens.py` writes
the real report when Kyle runs it. The template documents the report shape
so a reviewer knows what to expect.

**Tokenizer:** _<filled by script — qwen3-30b-a3b-instruct-awq preferred,
tiktoken cl100k_base fallback>_

**Static budget (plan §0b):** ≤ 1,000 tokens (warn) / ≤ 1,200 tokens (fail)

## Per-file token counts

| File | Constant | Tokens | Chars | tokens/char |
|---|---|---|---|---|
| `src/fastapi/app/agent/prompts/agent_system.py` | `AGENT_SYSTEM_PROMPT` | _N_ | _N_ | _0.NNN_ |
| `src/fastapi/app/agent/prompts/oiur_section.py` | `OIUR_SECTION` | _N_ | _N_ | _0.NNN_ |
| ... | ... | ... | ... | ... |

## Per-file totals

| File | Total tokens |
|---|---|
| `src/fastapi/app/agent/prompts/agent_system.py` | _N_ |
| ... | ... |

## Grand total: _N_ tokens

## Budget verdict

_PASS / WARN / FAIL based on the threshold checks._

## Plan §0b planned additions (NOT yet measured here)

These additions land on top of the current measurements above:

- `vocab_instructions` block — plan §1d-iv — estimated ≤ 200 tokens
- Structured answer format — plan §4a — estimated ≤ 250 tokens
- Anti-hallucination rules — plan §4a — estimated ≤ 150 tokens
- Dynamic `vocab_context` runtime injection — plan §0b — up to 640 tokens

Total projected static after additions: _current + 600_ tokens.
Total projected runtime: _current + 600 + 640_ tokens.

## What to do if WARN / FAIL

1. Identify the largest constants in the per-file table — those are the compression targets.
2. Move per-intent guidance into the retrieval profile (`retrieval_profile.py`) rather than the system prompt.
3. Compress `example_system.py` — a single curated example is enough.
4. Move structured-format scaffolding into the assembler; keep only the *rule* in the system prompt.

---

_Generated by `scripts/measure_system_prompt_tokens.py`. Re-run after any prompt change._
