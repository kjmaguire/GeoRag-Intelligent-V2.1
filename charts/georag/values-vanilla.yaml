# ──────────────────────────────────────────────────────────────────
# Vanilla Kubernetes tuning — assumes the cluster admin gives you a
# StorageClass + ingress controller (nginx by default; override if
# using HAProxy / Contour / Cloud LB).
# ──────────────────────────────────────────────────────────────────
global:
  # Leave empty to use the cluster default StorageClass.
  storageClass: ""
  ingressClass: nginx
  tier: medium

ingress:
  enabled: true
  className: nginx
  host: georag.example.com
  tls:
    enabled: true
    secretName: georag-tls
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod

# NetworkPolicy peer for the nginx controller (off by default — see
# values.yaml `networkPolicy.enabled`). Adjust if your controller runs
# elsewhere or carries different labels.
networkPolicy:
  ingressController:
    namespace: ingress-nginx
    podSelector:
      app.kubernetes.io/name: ingress-nginx

# Medium-tier sizing
postgresql:
  resources:
    requests: { cpu: "2", memory: "4Gi" }
    limits:   { cpu: "8", memory: "16Gi" }
