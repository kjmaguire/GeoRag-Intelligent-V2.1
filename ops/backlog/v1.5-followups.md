# V1.5 Follow-ups

Single source of truth for items deferred from the V1 production-hardening
pass (Modules 1-10, closed 2026-04-22). Replaces scattered "deferred to
V1.5" notes across module status memos.

When V1.5 work begins, this file is the punch list.

## Numbering

Stable IDs (`v1.5-NN`) so we can cross-reference from PRs and runbooks.

---

## Theme 1 — Test / CI completeness

### v1.5-01 — Playwright e2e CI job
- **Source:** Module 10 audit M-A4-05; `playwright.config.ts` exists.
- **Scope:** Author 5-10 e2e specs (chat → citation click → bubble render → feedback flow + map layer toggle + IDOR-safe project switching). Wire to PR-blocking job.
- **Blocker:** GitHub-hosted runners can't run Chromium reliably — needs self-hosted runner OR managed Browserless.
- **Effort:** 1-2 sessions.

### v1.5-02 — 24h soak test
- **Source:** Module 10 audit L-A8-06.
- **Scope:** Sustained-load script that runs `load_test.py` at moderate concurrency for 24h; emits memory + Redis + Postgres connection trends; alerts on slow leaks.
- **Effort:** 0.5 session for the script; 24h to run; ongoing operator review.

### v1.5-23 — Frontend localStorage token sweep
- **Source:** `resources/js/types.ts:11-12` flags `localStorage` as an XSS-exfiltration target. `ProjectContextBanner.tsx` fix (2026-04-22) established the `credentials: 'same-origin'` pattern and the auth-surface regression-test template.
- **Scope:** Migrate all remaining `localStorage.getItem('georag_token')` reads across the React frontend to Sanctum SPA session-cookie auth (`credentials: 'same-origin'`). Remove the now-unused `localStorage.setItem('georag_token', ...)` writer in `Login.tsx`.
- **Status:** CLOSED 2026-04-26. Migrated 17 call sites across 16 files:
  - `Layouts/AppLayout.tsx` (logout handler)
  - `Layouts/DashboardLayout.tsx` (fetchProjects)
  - `Hooks/Dashboard/useDashboardFetch.ts` (generic fetch hook)
  - `Pages/Chat.tsx` — 4 sites: `_authedFetch`, `runQueryHandshake`, `replayMissedEvents`, `SourceViewer.loadSource`
  - `Pages/Dashboard/ProjectAnalytics.tsx`
  - `Pages/NewProject.tsx`
  - `Components/StripLogViewer.tsx`
  - `Components/MapView.tsx` — 2 sites: `fetchCollars` and `transformRequest`
  - `Components/HoleDetailSheet.tsx` — 2 sites: collar index fetch and chat submit
  - `Components/HoleAnalysis/HoleAnalysisPanel.tsx`
  - `Components/DrillHoleBrowser.tsx`
  - `Components/ProjectSelector.tsx`
  - `Components/PublicGeoscience/CitationPGEODetail.tsx`
  - `Components/PublicGeoscience/EntityReferencesDrillIn.tsx`
  - `Components/chat/EvidenceInspector.tsx` (removed token from `getAuthHeaders`)
  - `Components/chat/FeedbackButtons.tsx`
- **Login.tsx writer:** Removed the entire `try { /api/v1/auth/login ... localStorage.setItem }` backward-compat block. No backend code referenced the token; Sanctum session is the canonical credential. `bootstrap.ts:58` `removeItem` kept as defensive cleanup for any pre-existing stored token (one-release grace).
- **TODO(security) left:** `EvidenceInspector.tsx:fetchEvidence` — `VITE_FASTAPI_URL` defaults to `/fastapi` (same-origin nginx proxy); if overridden to a full URL in a non-proxied deployment, `credentials: 'same-origin'` will not send the cookie. Documented inline; needs a service-to-service auth strategy if that env var is ever set.
- **Test deltas:** vitest 468 → 476 (+8 new auth-surface assertions across 4 existing test files: `EvidenceInspector.test.tsx`, `FeedbackButtons.test.tsx`, `CitationPGEODetail.test.tsx`, `EntityReferencesDrillIn.test.tsx`). Tests without existing test files noted in summary (no new test files created).
- **Files needing a follow-up test (no existing test file):** All 12 covered as of 2026-04-27. New test files:
  - `Layouts/__tests__/DashboardLayout.test.tsx` (2 tests — fetchProjects no-token + same-origin)
  - `Hooks/Dashboard/__tests__/useDashboardFetch.test.ts` (2 tests — renderHook no-token + same-origin)
  - `Pages/__tests__/Chat.test.tsx` (1 test — on-mount no-token; streaming paths not triggered in jsdom)
  - `Pages/Dashboard/__tests__/ProjectAnalytics.test.tsx` (2 tests — analytics fetch no-token + same-origin)
  - `Pages/__tests__/NewProject.test.tsx` (1 test — on-mount no-token)
  - `Components/__tests__/StripLogViewer.test.tsx` (2 tests — collar fetch no-token + same-origin)
  - `Components/__tests__/MapView.auth.test.ts` (3 tests — **static source inspection** via `?raw` import; maplibre-gl cannot render in jsdom)
  - `Components/__tests__/HoleDetailSheet.test.tsx` (2 tests — collar fetch no-token + same-origin; `georag_hole_chat_*` key confirmed non-token)
  - `Components/HoleAnalysis/__tests__/HoleAnalysisPanel.test.tsx` (2 tests — analysis fetch no-token + same-origin)
  - `Components/__tests__/DrillHoleBrowser.test.tsx` (2 tests — collar fetch no-token + same-origin)
  - `Components/__tests__/ProjectSelector.test.tsx` (2 tests — project fetch no-token + same-origin)
  - `AppLayout.tsx` — already had `Layouts/__tests__/AppLayout.test.tsx` (3 tests, pre-existing).
  **vitest count: 479 → 500 (+21 new assertions across 11 new files). tsc clean on all new files.**
- **Effort:** 1 session.

### v1.5-03 — JWT `kid` rotation header
- **Source:** Module 9 9.8 spec deferral; audit A4 backlog.
- **Scope:** Add `kid` header to Laravel-minted JWTs; FastAPI side picks signing key from a small kid→key map. Enables zero-downtime key rotation.
- **Effort:** 0.5 session.

---

## Theme 2 — Observability uplift

### v1.5-04 — Full Monolog JSON formatter on Laravel channels
- **Source:** Module 10 Chunk 10.6 deferral.
- **Scope:** Apply `Monolog\Formatter\JsonFormatter` to `single`, `daily`, `authz_audit` channels. Update Promtail pipeline regex → straight JSON parse.
- **Effort:** 0.5 session (config-only).

### v1.5-05 — `python-json-logger` formatter on FastAPI
- **Source:** Module 10 Chunk 10.6 deferral.
- **Scope:** Replace uvicorn default text formatter with `pythonjsonlogger.jsonlogger.JsonFormatter`. Keep `extra` field bridge.
- **Effort:** 0.5 session.

### v1.5-06 — Martin upgrade for `/metrics` endpoint
- **Source:** Memory `feedback_martin_tile_gotchas.md` Gotcha 3; Module 10 audit; 4 alert rules dormant until upgrade.
- **Scope:** Pin a Martin release (≥1.6.x) that exposes `/metrics`. Re-enable martin Prometheus alerts (drop the DORMANT annotations). Validate against current MVT functions.
- **Effort:** 0.5 session for the upgrade; carries some MVT-function regression risk → run pgTAP afterward.

### v1.5-07 — Neo4j metrics exporter (REVISED 2026-04-22)
- **Source:** Module 10 Chunk 10.4 — neo4j target DOWN.
- **REVISION:** original scope assumed `server.metrics.prometheus.*` config flags. Those are **Enterprise-only** in Neo4j 2026 — Community Edition rejects them at startup with strict-config validation (per the existing block comment in `docker/neo4j/conf/neo4j.conf:67-90`).
- **Real scope:** Author `scripts/neo4j_metrics_exporter.sh` that polls `CALL dbms.queryJmx(...)` over cypher-shell every 30s and writes Prometheus textfile-collector format to `/var/lib/node_exporter/textfile_collector/neo4j.prom`. Add a `node_exporter` sidecar to compose under `dev-monitor`. Update `prometheus.yml` to scrape node_exporter and label as `service: neo4j`.
- **Effort:** 1 session (the file-format spec + the JMX queries + sidecar wiring).
- **Alternative:** drop to Neo4j Enterprise license (free for dev; client-decision for prod). Out of free-licensing remit.

### v1.5-08 — Reverb + Dagster native exporters
- **Source:** Module 10 Chunk 10.4 — both targets DOWN.
- **Scope:** Reverb has no first-party exporter; will likely need a small sidecar that polls Reverb's admin endpoint. Dagster has community-maintained exporters; pick + integrate.
- **Effort:** 1-2 sessions (research + integration).

---

## Theme 3 — Frontend type safety

### v1.5-09 — `AlterationMap.tsx` `@ts-nocheck` removal
- **Source:** Module 10 Chunk 10.9 — confirmed 7 real type errors when stripped.
- **Scope:** Annotate `useRef<maplibregl.Map | null>(null)`, type the GeoJSON Feature[] returns, fix the LngLatBoundsLike call site. Mirrors `MapView.tsx` Module 8 Chunk 8.7 work.
- **Effort:** 1 session.

### v1.5-10 — `StripLogViewer.tsx` `@ts-nocheck` removal
- **Source:** Module 10 Chunk 10.9 — confirmed 13+ real type errors.
- **Scope:** Type the state hooks, fetch response assertions, lithology_logs/well_log_curves shapes. Likely needs new types in `resources/js/types.ts`.
- **Effort:** 1-2 sessions.

### v1.5-11 — Layer visibility localStorage
- **Source:** Module 8 Chunk 8.7 deferral.
- **Scope:** Persist `MVT_LAYERS` visibility across page loads via localStorage; restore on map mount.
- **Effort:** 0.5 session.

### v1.5-12 — DEM self-host
- **Source:** Memory `feedback_map_performance.md`.
- **Scope:** Self-host the DEM tiles currently served from CDN. Likely a Martin function or a separate static-tile container.
- **Effort:** 1 session (mostly storage + build).

---

## Theme 4 — Infrastructure breadth

### v1.5-13 — arm64 Docker image builds
- **Source:** Module 10 Chunk 10.2 deferral.
- **Scope:** `linux/arm64` added to `platforms` in build-push-action. Verify each base image has arm64 (postgis, neo4j, qdrant, ollama all do; vLLM is x86_64-only — flag).
- **Effort:** 1 session; vLLM exclusion needs design decision.

### v1.5-14 — k8s manifests / Helm chart
- **Source:** Module 10 Chunk 10.2 — explicitly out of V1 scope.
- **Scope:** Author Helm chart mirroring docker-compose.yml. Targets Rancher / k3s deployments for clients who want k8s.
- **Effort:** 2-3 sessions.

---

## Theme 5 — Carry-forward measurement / debug

### v1.5-15 — Module 1 C1 volume-wipe cold-start DR drill
- **Source:** Module 1 carry-forward.
- **Scope:** Execute the DR drill documented in `ops/runbooks/volume-migration.md` § Cold-start volume wipe. Captures real restore time + surfaces any backup gaps.
- **Effort:** 0.5 session (actual exec + writeup).

### v1.5-16 — Module 1 Neo4j 2026.03.1 drift
- **Source:** Module 1 carry-forward; image tag updated mid-session.
- **Scope:** Verify `docker-compose.yml` + arch doc reference the same Neo4j minor version. Mostly doc-update.
- **Effort:** 15 min.

### v1.5-17 — Module 4 Phase C + D (retrieval measurement + runbooks)
- **Source:** Module 4 carry-forward.
- **Scope:** Phase C: instrument retrieval-quality measurements (recall@5, MRR per fixture, BGE reranker delta). Phase D: author retrieval-tuning runbook.
- **Effort:** 1-2 sessions; partially absorbed by Module 10's perf baseline + retrieval-pipeline.md.

### v1.5-18 — Module 5 TOOL-CALL-01 investigation
- **Source:** Module 5 carry-forward.
- **Scope:** Investigate the tool-invocation regression flagged during Qwen 3 MoE migration. Likely now stale (re-validate against current orchestrator).
- **Effort:** 0.5 session.

### v1.5-19 — Module 7 STREAM-02 reconnect-replay closure
- **Source:** Module 7 Chunk 4 deferral.
- **Scope:** Refactor SSE wiring in `Chat.tsx` into a `useChatStream` hook so the reconnect-catchup closure reads the current `lastSeq` instead of a stale capture. Live-stream dedup already works — this only fixes catchup-after-disconnect.
- **Effort:** 1 session.

### v1.5-20 — FreshnessBadge workspace data_version diff
- **Source:** Module 7 Chunk 4 deferral.
- **Scope:** Wire `FreshnessBadge` to consume the workspace data_version endpoint that Module 9 9.4 enabled. Currently only uses `answered_at` age.
- **Effort:** 0.5 session.

---

## Theme 6 — Documentation closure

### v1.5-21 — Architecture doc v1.10 banner + 17 inline edits
- **Source:** Module 10 Chunk 10.9 deferral; 17 items in `ops/backlog/module-10-doc-sweep.md`.
- **Scope:** Reconcile banner versions (title v1.8 / intro v1.8 / footer v1.9 → all v1.10). Apply the doc-only edits (Neo4j label, Qdrant `sparse_vectors_config`, KML deprecation, §04j RESTRICT, §06/§12 PG tuning values, etc.). Embed `docs/acceptance-criteria.md` § §11.5 as a sub-section.
- **Effort:** 1-2 sessions; the doc is multi-thousand lines so this needs a focused editing pass.

### v1.5-22 — Kyle-decision items capture
- **Source:** `module-10-doc-sweep.md` 5 Kyle-decision items.
- **Scope:** Surface the open decisions: `DrillHole` label canonicalisation, `ef_construct` 200 vs 128, Dagster GPU access, and 2 others. Capture decisions inline in the arch doc.
- **Effort:** 0.5 session (mostly Kyle; engineering captures the decisions).

### v1.5-24 — KV-cache q8_0 re-validation under Qwen3 MoE
- **Source:** Qwen-setup review 2026-04-27.
- **Scope:** Sweep `OLLAMA_KV_CACHE_TYPE` across `f16`, `q8_0`, `q4_0` against the golden-query corpus on `qwen3:30b-a3b`. The "near-zero quality drop at q8_0" claim was measured on dense models — MoE attention may behave differently. Capture per-config golden-quality + tokens/sec + VRAM headroom; pick the q8_0/q4_0 cutoff and update the comment block in `docker-compose.yml` ollama service.
- **Effort:** 1 session — three runs of the existing validator, plus a short markdown report under `ops/baselines/`.
- **Runnable:** `ops/validation/qwen3_kv_cache_sweep.sh` orchestrates the three configs (compose override + container restart + warm-up + validator). Roughly 10-15 minutes against a warm dev workstation.

### v1.5-25 — Qwen3 sampling parameter validation
- **Source:** Qwen-setup review 2026-04-27.
- **Scope:** Confirm Qwen team's published sampling defaults (`top_p=0.8`, `top_k=20`, `min_p=0`, `presence_penalty=1.5` on no-think) actually improve grounded-synthesis quality vs the prior temperature-only call. Run golden-query MRR comparison with vs without the new params. If neutral, keep them (defensive); if regressive, back them out via the `QWEN3_*` env knobs.
- **Effort:** 0.5 session — one validator run, scored on the existing rubric.
- **Runnable:** `docker exec georag-fastapi python /app/ops/validation/qwen3_sampling_sweep.py` runs both configs (3 runs/prompt × 5 prompts × 2 configs ≈ 30 calls) and emits decision JSON + markdown under `ops/validation/reports/`.

### ~~v1.5-26 — Q5_K_M GGUF migration for qwen3:30b-a3b (MoE-correct quantisation)~~ **CLOSED 2026-05-08 — REJECT**
- **Verdict:** Stay on Q4_K_M default. Q5_K_M (`unsloth/Qwen3-30B-A3B-Instruct-2507-GGUF:Q5_K_M`) failed both gates on the actual A4500 hardware: −0.067 on the geo rubric (gate: ≥+0.05) and −70% throughput (101 → 31 tok/s) due to ~1.5 GB CPU offload from VRAM overflow.
- **Audit trail:**
  - `ops/validation/reports/qwen_moe_validation_a4500_baseline_20260508.json` (Q4_K_M baseline: 0.848 / 101.75 tok/s)
  - `ops/validation/reports/qwen_moe_validation_a4500_q5km_ab_20260508.json` (A/B run)
  - `ops/validation/reports/v1.5-26-q5km-decision.md` (full writeup with per-prompt comparison)
- **Followup:** Re-open if ≥24 GB VRAM lands for staging — at that point a clean Q5_K_M-of-same-base-fine-tune comparison becomes meaningful (the 2507 Instruct fine-tune is a confound on this round).

### ~~v1.5-27 — MoE telemetry sidecar live-fire~~ **CLOSED 2026-05-08 — sidecar live**
- **Status:** `georag-moe-telemetry` running on port 9479, scraping Ollama `/api/ps` every 15s. Live `qwen_moe_offload_layers_cpu` correctly tracks the qwen3:30b-a3b Q4_K_M offload (~825 MB / 4.2% of layers) when the model is resident, returns to 0 when KEEP_ALIVE expires.
- **Wired:** `docker/prometheus/prometheus.yml` got the `moe-telemetry` scrape job inline (Prometheus container itself isn't currently in the dev profile — the sidecar emits metrics regardless and the scrape target lights up when Prometheus comes online).
- **Open:**
  - Grafana panel for VRAM/CPU split + KV-cache pressure (deferred until Prometheus is in the dev profile).
  - Per-expert routing metrics (`qwen_moe_router_entropy_top2`, `qwen_moe_expert_token_total`, `qwen_moe_expert_active_ratio`) emit no-ops until the upstream llama.cpp router-stats build lands. Layer-1 metrics (offload, KV pressure, scrape failures) are live now.
  - Alert rule smoke-fire still pending (Alertmanager isn't in the dev profile either).

### v1.5-28 — Ollama tier routing on staging hardware
- **Source:** Qwen-MoE expert review 2026-05-08, item #3.
- **Scope:** Once ≥24 GB GPU hardware is online (L4 / A5000 / A6000 / L40S), pull `qwen3:8b` + `qwen3:14b`, set `OLLAMA_MAX_LOADED_MODELS=2`, flip `OLLAMA_TIER_ROUTING_ENABLED=true`, run a load-test against the dev workstation's reference workload mix (45% factoid). Expect ~2× sustained concurrency improvement. Code path is wired (`tier_to_model_for_backend` in `src/fastapi/app/agent/model_routing.py`) and unit-tested; flipping the flag exercises it.
- **Blocked on:** ≥24 GB VRAM. **Cannot run on the actual dev workstation** (RTX A4500 20 GB) — qwen3:8b (~5 GB) + qwen3:30b-a3b Q4_K_M (~18 GB) = 23 GB exceeds VRAM. See capacity-planning.md hardware-refresh note.
- **Effort:** 0.5 session post-hardware (env flip + load-test + capacity-planning update).

---

## Operator setup (blocks first prod deploy, not V1.5 engineering)

These are documented in `docs/acceptance-criteria.md` and listed here for
completeness. They're **not** engineering deliverables — Kyle (or ops)
performs them before first prod cutover.

- O-01 Generate `SOPS_AGE_PRIVATE_KEY` GitHub Secret
- O-02 Configure `DEV_SSH_HOST` / `STAGING_SSH_HOST` / `PRODUCTION_SSH_HOST` (+ user/key)
- O-03 Configure `STAGING_URL` for release-rehearsal + perf-baseline
- O-04 Encrypt initial `.env.production.enc` from `.env.production.example`
- O-05 Run cold-start migration per `ops/runbooks/cold-start.md`
- O-06 Capture first perf-baseline run as the regression threshold
- O-07 Wire Alertmanager webhook to ops channel (Slack/PagerDuty)

---

## Suggested V1.5 sequencing

If picking themes vs. one-by-one:

**Sprint 1 — Observability uplift** (Theme 2)
- v1.5-04, v1.5-05 (logging JSON polish — fast, low risk)
- v1.5-06 (Martin upgrade)
- v1.5-07 (Neo4j JMX — 15 min)
- v1.5-08 (Reverb + Dagster exporters)

Total: 3-4 sessions. Outcome: every Prometheus target UP, full JSON logs, Martin alerts active.

**Sprint 2 — Test breadth** (Theme 1)
- v1.5-01 (Playwright e2e — needs self-hosted runner)
- v1.5-03 (JWT kid)
- v1.5-02 (soak test scaffold; let it run for 24h)

Total: 2-3 sessions.

**Sprint 3 — Frontend type safety** (Theme 3)
- v1.5-09, v1.5-10, v1.5-11, v1.5-12

Total: 3-4 sessions.

**Sprint 4 — Carry-forward + docs** (Themes 5 + 6)
- v1.5-15 through v1.5-20 (most are <1 session each)
- v1.5-21 + v1.5-22 (arch doc rev with Kyle's input)

Total: 4-5 sessions.

**Sprint 5 — Infrastructure breadth** (Theme 4)
- v1.5-13 (arm64)
- v1.5-14 (k8s manifests if any client demand)

Defer indefinitely if no client demand.

---

## Status header (update as items close)

```
v1.5 progress: 23 / 23 items closed (engineering scope — operator validation pending where flagged). Sprint 1 DONE; Sprint 2 DONE; Sprint 3 DONE; Sprint 4 DONE; Sprint 5 DONE.

  ✅ v1.5-01 — Playwright e2e CI workflow. New `.github/workflows/e2e.yml` (PR-blocking on
              UI-relevant paths + workflow_dispatch + Mon 03:00 UTC cron). Targets
              self-hosted runner labelled `georag-e2e` with full Compose stack boot,
              migrate + seed, npm ci + Playwright Chromium install, suite run, artifact
              upload on failure, teardown. Skip-gate `runner-check` short-circuits the
              suite cleanly until the runner is provisioned (no false greens, no failures).
              Existing spec `tests/e2e/public-geoscience-popup.spec.ts` covers the
              click→popup invariant. **Operator-blocked on:** runner provisioning + repo
              secret CI_DEMO_PASSWORD. Spec count expansion (chat→citation, layer toggle)
              left for first-run-feedback iteration after the runner lands.

  ✅ v1.5-02 — 24h soak test driver. New `src/fastapi/scripts/soak_test.py` (~200 lines):
              wraps `load_test.py` in a sustained moderate-concurrency loop with
              `--cycle-seconds 600` sampling, captures p95 per query class + FastAPI
              `process_resident_memory_bytes` from `/metrics`, writes per-cycle TSV to
              stdout + machine-parseable `soak-trend.json` on exit, applies conservative
              growth thresholds (RSS +25%, p95 +20%, Redis keys +50%, PG conns +30%) and
              prints SOAK PASS / SOAK FAIL verdict with offending flags. SIGINT-clean.
              **Operator-blocked on:** STAGING_URL + 24h window to actually run.

  ✅ v1.5-13 — arm64 Docker image builds. `.github/workflows/ci.yml` `docker-build` matrix
              now builds `linux/amd64,linux/arm64` for all three georag/* images
              (fastapi/laravel/dagster — all Python/PHP, arm64-clean). vLLM prod-tier
              caveat documented inline (vLLM is x86_64-only; arm64 hosts run Ollama only).
              Cross-referenced from the Helm chart README (v1.5-14). First arm64 push
              validates on next merge to `main`.

  ✅ v1.5-14 — Kubernetes Helm chart skeleton. New `ops/charts/georag/`:
                • `Chart.yaml` (v0.1.0; explicit "starting point not production-ready"
                  framing + maturity-gate list).
                • `values.yaml` (~150 lines mirroring docker-compose.yml env surface;
                  app services in-chart, datastores externalised under `externalServices.*`
                  to plug into community charts).
                • `templates/_helpers.tpl` (naming + image helpers).
                • `templates/laravel-octane.yaml` (Deployment + Service + livenessProbe
                  /up + secrets via secretRef).
                • `templates/fastapi.yaml` (Deployment + Service + livenessProbe /healthz
                  + JWT kid env wiring).
                • `templates/config.yaml` (ConfigMap with PG/Redis/Neo4j/Qdrant/S3/Ollama/
                  Martin connection details + Secret with APP_KEY + JWT keys).
                • `README.md` (install + 8-row maturity-gate table + arm64 caveat).
              Datastores deliberately NOT bundled — operators bring postgres/redis/etc
              via bitnami or equivalent. Horizon/Reverb/Dagster have value-stubs but no
              templates yet (extension point for first client demand). **Production gate:**
              network policies, HPA/PDB, PVC strategy, cert-manager, monitoring subcharts,
              SOPS secrets — all flagged in chart README. Until those land the chart is
              for demo / eval, not prod cutover.

  ✅ v1.5-15 — Cold-start volume-wipe DR drill. **No engineering work** — runbook already
              exists at `ops/runbooks/volume-migration.md` (authored Module 10 Chunk 10.8).
              Drill execution is a pure operator action gated on a staging environment with
              representative volume sizes. Item closed-as-already-shipped; first execution
              tracked separately as a deployment milestone.

  ✅ v1.5-22 — Kyle-decision items capture. New `docs/kyle-decisions.md` (~150 lines)
              capturing 6 architectural decisions deferred to owner sign-off:
                • D1 Qdrant `ef_construct` 200 vs 128 (currently 200 live; defensible
                  either way — recommend keep)
                • D2 Neo4j `DrillHole` (spec) vs `Drillhole` (live, 33510 nodes)
                  canonicalisation (recommend rename live to match spec)
                • D3 Dagster GPU access for SPLADE prod (recommend defer to client demand)
                • D4 Neo4j heap-restart timing (recommend next backup window)
                • D5 Neo4j retrieval timeout 2s vs 3s (recommend update spec to 2s)
                • D6 Architecture-doc addendum sweep — keep in audit/backlog vs fold into
                  HTML (recommend keep separate)
              Each item has current state + 2-3 options with cost/tradeoff + recommendation.
              "Resolved" section at the bottom is empty; append decisions as they land.



  ✅ v1.5-21 — Architecture doc v1.10 banner + inline edits (mechanical reconciliation pass).
              `georag-architecture.html` updated:
                • Banner unified to v1.10 (title + intro provenance notice + footer; v1.9 added
                  an explicit "evidence-model + RBAC + map/tile + observability passes" bullet,
                  v1.10 marks the V1 production-hardening close-out).
                • §04d KML/KMZ moved out of supported formats, listed as deferred V1 roadmap.
                • §04i workspace_id population responsibility split clarified (Module 3 writes,
                  Module 9 enforces, Module 2 Phase B installed Qdrant payload index).
                • §05c cache key composition now lists `routing_categories_hash` component.
                • §06 PG memory tuning shows two-tier (moderate 8GB / aggressive 16GB).
                • §06 `io_method` carries the Docker/WSL2 seccomp caveat (default = `worker`).
                • §06 Qdrant row documents live `ef_construct=200` with recall/cost rationale.
                  ⚠ This was on the Kyle-deferred list (D1) but agent picked "doc-matches-live"
                  interpretation. Defensible (the alternative was to recreate the 5 live
                  collections at lower ef). If Kyle prefers 128, schedule collection rebuild
                  and revert this line — note tracked in v1.5-22 (Kyle decisions).
                • §11 deployment block: `APP_DEBUG=false` mandatory for prod/staging compose.
                • §11b appendix link to `docs/acceptance-criteria.md` for the V1 ship checklist.
                • §12 PostgreSQL row → 18.3, Martin row → 1.7.0 (with `/_/metrics` note).
              **Skipped (target text not in base HTML — needs addendum-doc pass)**:
                Neo4j retrieval timeout 3.0→2.0 (no timeout text in §06); query-class precedence
                (no explicit ordered list in doc); Qdrant `sparse_vectors_config` (PATCH example
                not present); §04j FK RESTRICT + bronze.provenance/document_revisions paragraph +
                B8 enable-order list (§04j is an addendum section not in the base HTML).
              The skipped items are **doc-only** and ride into v1.5-22 / next addendum-doc edit.



  ✅ v1.5-17 — Module 4 Phase C + D close-out.
              **Phase C (measurement):** two new integration tests added to
              `src/fastapi/tests/test_retrieval_quality.py` — `test_reranker_lift_averaged`
              issues each RETRIEVAL_CASES query, captures `answer_run_id` from the SSE
              stream, joins `silver.answer_retrieval_items` ↔ `silver.document_passages`
              for substring-match against `expected_in_top5`, computes MRR for the
              `retrieved` (RRF-only) vs `reranked` (post-BGE) stages, and asserts
              reranker MRR ≥ retrieved MRR × 0.95 (5 % regression tolerance for noise).
              `test_per_class_mrr_visibility` prints per-case rank tables to stdout for
              CI log visibility (no hard floor — perf-baseline.yml owns numerical floors).
              Both gated `@pytest.mark.integration` so the fast suite is unaffected.
              **Phase D (runbook):** new `ops/runbooks/retrieval-tuning.md` (~210 lines):
              12-knob inventory (RSV / RRF k / prefetch / reranker top-k / timeouts /
              identifier-boost regex / relevance gate / SPLADE / BGE / dense embedding),
              decision tree mapping symptoms (recall@5 drop / reranker MRR negative /
              latency over budget / stale answer) to knobs, the standard 7-step tuning
              procedure (single-knob PRs, RSV bump policy, integration suite + golden +
              perf-baseline gates, revert-not-fix-forward on regression), how-to-read
              guidance for the new test stdout, and 4 anti-patterns (single-case tuning,
              gate-lowering, prefetch-without-top-k, model-swap-without-reingest).
              Cross-links to retrieval-pipeline / retrieval-cache / refusal-rate-spike /
              hybrid-retrieval / baseline doc. Operational debugging stays in
              retrieval-pipeline.md (already authored Module 4 Phase D); retrieval-tuning
              is the **decision** layer above it.
              Module 10's perf-baseline workflow + retrieval-pipeline.md absorbed half
              of the original Phase C+D scope; this closes the rest. Closes Module 4
              carry-forward.

  ✅ v1.5-18 — Module 5 TOOL-CALL-01 verify-and-close. Per `project_module_5_status.md`,
              the regression was resolved 2026-04-21 (root cause: NOT a tool-calling bug —
              was Qwen 3 thinking-mode budget exhaustion at OLLAMA_NUM_CTX=8192; fix was
              `enable_thinking=False` on the 3 grounded-synthesis call sites + raise
              context to 16K + add empty-content guard returning structured fallback).
              Re-validated against current orchestrator: `enable_thinking=False` confirmed
              live at `src/fastapi/app/agent/orchestrator.py:4052`, `:4134`, `:4194`;
              `budget_exhausted_by_thinking` log guard at `:1652`; investigation doc
              `ops/audit/2026-04-21-tool-call-01-investigation.md` on disk. Nothing to
              fix — backlog item was a tracking placeholder. Closes Module 5 carry-forward.

  ✅ v1.5-12 — DEM self-host as configuration. `MapView.tsx` DEM + satellite tile URLs are now
              Vite env-overridable: `VITE_DEM_TILES_URL` / `VITE_SATELLITE_TILES_URL` fall back
              to the existing public CDNs (`tiles.mapterhorn.com` / `tiles.maps.eox.at`) when
              unset, so dev keeps free tiles and air-gapped/private deployments point at their
              own endpoints with zero code change. Both keys added (commented) to `.env.example`
              and (with self-hosted placeholder URLs) to `.env.production.example`.
              New runbook `ops/runbooks/dem-self-host.md` (~230 lines): data-source comparison
              (SRTM / AW3D30 / NRCan CDEM / Copernicus DEM), `rio rgbify` terrain-RGB encoding,
              two serving patterns (Martin raster_sources preferred, nginx static fallback),
              storage budget table, satellite section (EOX Sentinel-2 / Sentinel-2 L2A raw /
              Maxar / Landsat), 10-step operator checklist, "when NOT to self-host" guidance.
              Closes the deferred concern in `feedback_map_performance.md`. The actual data-
              acquisition + tile-encoding work is operator effort gated by client geography —
              the engineering side (hooks + docs) is what was missing; both now landed.
              0 TS errors, 466/466 vitest unchanged.

  ✅ v1.5-11 — Layer visibility localStorage. New `resources/js/lib/layerVisibilityStorage.ts`
              (read/write/merge helpers; v1-prefixed key for forward-compat; tampered values
              dropped at read time; missing layers fall through to default visibility). MapView
              `useState` initialiser hydrates from localStorage; `useEffect([visibleLayers])`
              persists every change. Best-effort — quota errors + private-browsing throws are
              swallowed so the in-memory toggle still updates. 12 new vitest cases in
              `resources/js/lib/__tests__/layerVisibilityStorage.test.ts`. 454 → **466 vitest**.

  ✅ v1.5-16 — Neo4j 2026.03.1 doc drift. `georag-architecture.html` §12 row updated:
              `Pinned: 2026.02.3` → `2026.03.1`, with a footnote that 2026.02.3 was never
              published to Docker Hub (the floating tag resolved to .03.1). Also notes that
              JMX page-cache + transaction beans are Enterprise-only on this release; the
              v1.5-07 sidecar uses `java.lang:*` JVM beans only. Doc-only edit; no code change.
  ✅ v1.5-04 — Laravel Monolog JsonFormatter on single/daily/authz_audit channels.
              Promtail pipeline simplified to `| json` parse, `datetime` timestamp.
              Verified: tail of authz_audit-*.log emits proper JSON shape.
  ✅ v1.5-05 — FastAPI JSON formatter at src/fastapi/app/logging_config.py.
              `configure_json_logging()` invoked at module import in main.py.
              6/6 unit tests green; live FastAPI logs confirmed JSON.
  ✅ v1.5-06 — Martin 1.5.0 → 1.7.0 (digest sha256:077329dbde8d...).
              Prometheus scrape `/_/metrics` (note the /_/ prefix — reserved-keywords namespace).
              martin-alerts.yml rewritten with real metric names: martin_http_requests_duration_seconds,
              martin_tile_cache_requests_total{cache,result,zoom}, martin_cache_requests_total.
              4 alerts no longer dormant. Memory `feedback_martin_tile_gotchas.md` Gotcha 3 marked RESOLVED.
              §05d (mvt, etag_hash) two-column return signature auto-discovered by 1.7.0; pgTAP 199/199 unchanged.
  ✅ v1.5-08 — Reverb + Dagster metrics surfaced through the existing Laravel /metrics endpoint
              instead of standalone exporters. Reverb has no Pusher-HMAC-free admin API (skipped sidecar);
              Dagster webserver returns HTML, not Prometheus. New series in MetricsController.php:
                - `dagster_runs_total{status="..."}` — direct PDO query against georag_dagster.runs
                  (bypasses pgbouncer because pgbouncer doesn't proxy the dagster DB).
                - `reverb_broadcasts_total` — Pulse-derived broadcast count (cache_set rollup over 5m).
              Dropped redundant `reverb` + `dagster` scrape jobs from prometheus.yml (data flows through
              laravel-octane scrape now). Net Prometheus targets: 11 → 9, with 8 of 9 UP (was 7 of 11).
              Live verification: dagster_runs_total emits {started=1, canceled=3, failure=22, success=27}.
  ✅ v1.5-07 — Neo4j JMX → Prometheus sidecar at `docker/neo4j-exporter/`. Hand-rolled Python
              (~140 lines) using the official neo4j driver — polls JMX over Bolt every 30s,
              serves Prometheus exposition on :9105/metrics. Compose service `neo4j_exporter`
              under dev-monitor profile, Dockerfile is python:3.13-slim + `neo4j>=5.27`,
              runs as nobody (UID 65534). Prometheus scrape job `neo4j` retargeted from
              `neo4j:2004` (Enterprise-only port) to `neo4j_exporter:9105`.
              Discovery: `org.neo4j:*` JMX namespace is **also Enterprise-only** in 2026
              Community Edition; only `java.lang:*` JVM beans are accessible. Surfacing
              10 metrics: neo4j_up, heap (used/max/committed), threads (live/peak/daemon),
              uptime_seconds, open + max file descriptors. Composite-attribute extractor
              peels two layers (`value.properties` for HeapMemoryUsage etc).
              **9/9 Prometheus targets UP after Sprint 1.**
  ✅ v1.5-09 — `AlterationMap.tsx` `@ts-nocheck` removal (7 errors fixed).
              `useRef<maplibregl.Map | null>(null)` + `useRef<HTMLDivElement | null>(null)` typed.
              New `CiaFeature = GeoJSON.Feature<Point, {hole_id, cia_mean, sample_count}>` interface
              replaces `unknown[]` feature list. `bbox` typed as `[[number,number],[number,number]] | null`
              for `LngLatBoundsLike` compat. `getSource` narrowed via `instanceof maplibregl.GeoJSONSource`
              for `setData` access. 0 TS errors, 454 vitest unchanged.
  ✅ v1.5-10 — `StripLogViewer.tsx` `@ts-nocheck` removal (24 errors fixed).
              New local interfaces: `LithologyLog`, `WellLogCurve`, `CollarRecord`, `StripLogViewerProps`,
              `TooltipPos`. State hooks typed (`useState<CollarRecord | null>`, etc.). Sub-component props
              annotated. `error.message` access guarded with `err instanceof Error` narrow. SVG-element
              arrays typed `React.ReactElement[]`. `points: string[]` for SVG polyline. `parseFloat()`
              calls dropped (the underlying values are already `number` — `.toFixed()` directly).
              `LithologyLog.from_depth` added (actual API shape; the silver-table-named `depth_from_m`
              kept as forward-compat). 0 TS errors, 454 vitest unchanged.
  ✅ v1.5-19 — STREAM-02 reconnect-replay closure (Module 7 Chunk 4 carry-forward).
              Surgical fix in `resources/js/Pages/Chat.tsx`: previous code destructured the
              `useEventDedup` hook return without keeping the object, then read `lastSeq` from a
              fake `lastSeqRef = { current: 0 }` placeholder that was never populated. Replaced
              with `const dedup = useEventDedup(...)` + `dedup.lastSeq` direct access — the hook
              already exposes lastSeq as a live getter on the return object. Simpler than the
              originally-spec'd `useChatStream` extraction; closes the actual bug.
              No new tests needed (existing useEventDedup unit tests already exercise the getter).
              446/446 vitest unchanged.
  ✅ v1.5-20 — FreshnessBadge workspace data_version diff (Module 7 Chunk 4 carry-forward).
              `computeStaleness` now takes optional `(queryDataVersion, currentDataVersion)`
              args. When both present AND current > query → forces 'stale' verdict regardless
              of clock age (data_version diff is the stronger signal — corpus changed since the
              answer was generated). Falls back to wall-clock age when either version is null.
              `FreshnessBadge` reads `workspace.data_version` from Inertia's `usePage<PageProps>()`
              shared props (Module 8 Chunk 8.5 wired this — no new HTTP endpoint needed).
              Removed the file-level `// @ts-nocheck`; types clean.
              8 new tests (5 computeStaleness scenarios + 3 render-path scenarios with mocked
              usePage). 446 → **454 vitest** passing.

  ✅ v1.5-03 — JWT `kid` rotation. Laravel `FastApiJwtMinter::mint()` now stamps every JWT with
              `kid: <FASTAPI_SERVICE_KEY_KID>` (default `primary`). FastAPI `extract_user_context`
              reads the kid via `jwt.get_unverified_header()`, picks the matching secret from a
              `{primary: KEY, previous: PREVIOUS_KEY}` map, then verifies the signature. Missing
              kid (legacy minter) falls back to primary; unknown kid → 401.
              New settings: `FASTAPI_SERVICE_KEY_KID`, `FASTAPI_SERVICE_KEY_PREVIOUS`,
              `FASTAPI_SERVICE_KEY_PREVIOUS_KID`. Both `.env.example` files updated. Updated
              `ops/runbooks/secret-rotation.md` § FASTAPI_SERVICE_KEY with the 6-step kid-based
              rotation procedure (replaces the old "fail-and-pray" single-key swap). 4 new
              FastAPI tests (primary kid + previous kid + unknown kid + no-kid fallback) +
              2 new Laravel tests (default kid + configured kid). 16/16 JWT tests green +
              6/6 Minter tests green. Closes Module 9 9.8 carry-forward; the JWT `kid`
              header was deferred to Module 11/V1.5 in the original audit.

Last updated: 2026-04-26 (22/22 engineering scope closed + nice-to-haves applied).

  ✅ **Nice-to-haves wrap-up** (2026-04-26):
     • **D1, D3, D4, D5, D6 resolved** in `docs/kyle-decisions.md` per recommended directions
       (D1 ef_construct=200 canonical; D3 CPU rates accepted for V1; D4 next backup window;
       D5 spec aligns to code at 2.0s; D6 keep addendum content in audit/backlog files).
       Only **D2 (Drillhole rename)** still pending — explicit owner sign-off required (Global
       Invariant 4 schema change).
     • **Helm prod-maturity gates templated**: 5 of the 8 gates closed at chart layer:
       - `templates/networkpolicy.yaml` — default-deny baseline + per-component allow rules
         for laravel-octane (ingress from ingress-nginx; egress to fastapi + datastores) and
         fastapi (ingress from laravel-octane; egress to LLM + datastores).
       - `templates/hpa.yaml` — HPA v2 for laravel-octane + fastapi with CPU/memory targets +
         scale-up/scale-down stabilization windows.
       - `templates/pdb.yaml` — PodDisruptionBudget for both deployments.
       - `templates/ingress.yaml` — cert-manager-annotated ingress with TLS toggle.
       - `templates/servicemonitor.yaml` — Prometheus Operator ServiceMonitor for /metrics
         endpoints (FastAPI + Laravel `/metrics` from Module 10 Chunk 10.4).
       All toggle off by default for skeleton install; flip in prod values.yaml. Remaining 3
       TODO gates (PVC SeaweedFS, Loki/Promtail subcharts, SOPS secrets) are deliberately
       externalised to community charts and helm-secrets plugin — not chart-internal scope.
       README updated with templated/TODO matrix + 4-step production rollout sequence.
     • **e2e spec expansion**: 3 new specs joining the existing public-geoscience-popup:
       - `tests/e2e/chat-citation.spec.ts` — chat → SSE → citation marker click → evidence
         Popover renders with source_chunk_id + chunk text; refusal-path no-grade-leak check.
       - `tests/e2e/map-layer-toggle.spec.ts` — layer toggle persists across hard reload via
         the v1.5-11 localStorage key; tampered-value fallback also exercised.
       - `tests/e2e/project-switch-idor.spec.ts` — Module 9 9.4 IDOR check via direct fetch
         to /api/queries + /api/answer-runs/<foreign-id>/citations with credentials; asserts
         403/404/422 (no leak).
       Specs use semantic role/aria queries (no brittle CSS selectors); will run when the
       self-hosted runner provisions.


```
