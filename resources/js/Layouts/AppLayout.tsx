/**
 * AppLayout — preserved as a shim for back-compat (rev 7).
 *
 * The legacy gray-900/amber shell has been replaced by FoundryShell (the
 * dark tactical layout from the Claude Design handoff). All existing
 * `import AppLayout from '@/Layouts/AppLayout'` callsites resolve here.
 *
 * If you want to inspect the old shell, see git history before this
 * commit (Wave 0).
 */
export { default } from './FoundryShell';
